jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'
      
      - name: Install OCR and Popplerjobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Install Tesseract OCR and Poppler
        run: |
          sudo apt update
          sudo apt install -y tesseract-ocr
          sudo apt install -y libtesseract-dev
          sudo apt install -y poppler-utils  # This installs Poppler and its utilities like pdfinfo

      - name: Verify Poppler Installation
        run: |
          pdfinfo -v  # Checks the version of pdfinfo to confirm Poppler is in PATH

      - name: Check PATH and locate pdfinfo
        run: |
          echo "Current PATH: $PATH"
          which pdfinfo  # Locates the pdfinfo executable to ensure it's in PATH

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: List files in current and pages directory
        run: |
          echo "Listing current directory contents:"
          ls -l
          echo "Listing pages directory contents:"
          ls -l pages/

      - name: Run tests or scripts
        run: python your_script.py

        run: |
          sudo apt update
          sudo apt install -y tesseract-ocr
          sudo apt install -y libtesseract-dev
          sudo apt install -y poppler-utils  # This line installs Poppler

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run tests or scripts
        run: python your_script.py
